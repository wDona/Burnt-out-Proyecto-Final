CREATE TABLE Note(
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    content TEXT NOT NULL
);

insertNote:
INSERT OR IGNORE INTO Note(id, content) VALUES (?, ?);

selectAllNotes:
SELECT * FROM Note;
